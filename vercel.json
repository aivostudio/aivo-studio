export default function handler(req, res) {
  return res.status(200).json({
    ok: true,
    route: "/api/music/status",
    method: req.method,
    query: req.query || null,
    ts: Date.now(),
  });
}
